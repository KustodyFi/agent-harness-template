---
description: Build and run the project locally
---

# /run — Build & Run

## Steps

1. Build the project (see `/build`)
2. Run the dev server or entry point
3. Report the output

## Rules

- Never hardcode credentials in run commands
- Use environment variables for secrets
